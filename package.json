{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "My personal website with npm and sass",
  "main": "index.html",
  "scripts": {
    "start": "npm-run-all --parallel sass serve",
    "sass": "node-sass -w --output-style compressed scss/styles.scss docs/styles.css --source-map true",
    "serve": "serve docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.0",
    "npm-run-all": "^4.1.5",
    "serve": "^11.3.0"
  }
}
